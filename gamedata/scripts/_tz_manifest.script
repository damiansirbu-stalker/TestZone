--- _tz_manifest: TestZone identity. Pure data, no logic. Read by _tz_init (the gate) and the validator (deps.manifest_structure).

MANIFEST = {
    name    = "TestZone",
    version = "1.0.4-snapshot",
    xlibs   = "1.5.1",
}
